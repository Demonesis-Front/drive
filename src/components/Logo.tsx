import React from "react";
import { LogoContainer } from "components/typography";
import { TEXT } from "constants/text";

export const Logo = () => {
	return <LogoContainer>{TEXT.logo}</LogoContainer>;
};
